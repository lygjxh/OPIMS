# 变更记录

本文件是 `for-claude` 分支的改动流水账，读者有两类：项目负责人、以及另一个
接手改代码的 AI 模型。**按时间倒序排列，最新的在最上面**——拉取代码后只看
最上面一段即可知道这次变了什么。

每条记录包含：改了什么 / 为什么这么改 / 涉及哪些文件 / 有什么坑要注意。
**重点看「为什么」和「注意」**，避免把已经修好的东西又改回去，或踩同样的坑。

> 协作约定：本分支是代码的唯一真相来源。改动只在 `for-claude` 上做，
> 不直接改 master。每次推送的都是「构建通过 + 实测验证过」的状态，
> 不会推中间态。

---

## 2026-07-27 · 新增「国别档案」模块（一期）

`e18bf60`

**改了什么**：新增模块，在 OPIMS 中展示 Obsidian 维护的各国出入境政策。
侧边栏「主业务」下新增「国别档案」入口，路由 `/country-profile`。

**数据来源**：OPIMS 根目录下的 `08.Policy/出入境政策/`，即用户的 Obsidian
政策库。每个国别一个 `.md`，格式为 YAML frontmatter（17 个字段）+ 十节正文。

**为什么这么设计**：
- **只读、不入库**：OPIMS 侧不提供编辑界面，也不把内容复制进数据库。
  用户继续在 Obsidian 里维护（那是趁手的工具），OPIMS 每次请求现读文件，
  永远是最新内容。避免两套数据双向同步的复杂度。
- **不新增配置项**：政策库位置由「根目录 + 固定子目录常量」推导，
  与既有 `ProjectsDir()`/`QualDir()` 同一模式。换电脑或移动文件夹只需重设
  根目录，政策库自动跟随；后续鸿蒙移植也无需额外处理路径。

**涉及文件**：
| 文件 | 作用 |
|---|---|
| `backend/services/rootdir.go` | 新增 `PolicySubdir="08.Policy"` 常量与 `PolicyDir()`/`EntryExitPolicyDir()` |
| `backend/services/policy.go` | 拆 frontmatter、goldmark(GFM) 渲染正文、风险等级提取、时效判断 |
| `backend/handlers/policy.go` | 三个接口 |
| `backend/data/country_alias.go` | 国别名归一化映射 |
| `backend/services/policy_test.go` | 4 个测试 |
| `frontend/src/views/CountryProfile.vue` | 列表卡片 + 详情抽屉 + 内部制度抽屉 |

**新增接口**：
- `GET /api/policy/countries` —— 各国档案摘要列表
- `GET /api/policy/country/{name}` —— 单国完整档案（name 用标准名或原始名都行）
- `GET /api/policy/internal` —— 公司内部管理制度摘要

**⚠️ 注意事项（改这块代码前务必看）**：
1. **`PolicyDir()` 故意指向 `08.Policy` 而不是直接指向「出入境政策」**。
   因为 `08.Policy` 是 Obsidian vault 根，以后会并列增加税务政策、劳工法规等
   分类，届时直接复用 `PolicyDir()` 即可，不用再改 rootdir.go。
2. **国别名两侧不一致，必须归一化**。Obsidian 用「印度尼西亚」「阿联酋-阿布扎比」，
   OPIMS 标准名是「印尼」「阿联酋」（见 `services/country.go`）。不归一化就无法
   与项目数据关联。新增国别档案时若两侧写法不同，去 `data/country_alias.go` 补一条。
3. **风险等级不能用等号匹配**。实际值形如 `高（重要提示：普通劳务人员在印尼
   法律下原则上没有合法工作签证路径…）`，是「等级 + 说明」的自由文本。
   现在按前缀取「高/中/低」用于着色，完整原文单独展示。
4. **必须忽略隐藏目录**。`08.Policy` 下有 `.obsidian` 配置目录，扫描时
   跳过 `.` 开头项；同时跳过 `00`-`02` 辅助文件（约定：数字开头即辅助文件），
   `03 公司内部管理制度摘要` 单独走 `/api/policy/internal`。
5. **风险等级配色必须配文字标签**，不能只靠颜色（红绿在红绿色盲下无法分辨，
   项目既有约定，见 requirement.md「配色与无障碍要求」）。
6. 目录不存在时接口返回 404 + `expected_dir`，前端据此显示引导页而不是白屏。
   改接口时别把这个错误结构去掉。

**二期待做**：项目详情弹窗的「国别」字段做成跳转链接；首页地图点击国家看政策摘要。

---

## 2026-07-27 · 修复黑名单联动测试

`01dce3a` —— **只改测试，未动业务代码**

**问题**：`go test ./services/` 两个测试失败。

**根因**：2026-07-26 的「分包商库单 Sheet 31 列重构」删除了
`subcontractors_base` 的 `registration_type` / `parent_short_name` 列，
级联关系改为 `assoc_unit`（关联单位），但 `blacklist_sync_test.go` 没跟着改。

**⚠️ 特别注意——报错信息是误导的**：
第二个测试报 `BUG: COUNT(*)->bool scan fails when >1 active entries`，
看起来像业务代码有 bug。**实际不是**。业务代码用的是 `var activeCount int` +
`COUNT(*)`，写法本来就正确。真实原因是测试插入旧列时用 `Exec` 却没检查
error，插入静默失败 → 查不到关联单位 → 级联不执行 → 断言失败。

**改法**：插入语句改用新表结构；`project_subcontract.sub_name` 改填全名
（与 `blacklist_sync.go` 中 `SELECT full_name` 的匹配口径一致）；所有插入
改为检查 error，避免再次静默失败误导判断。

**结论**：修完 4 个测试全过，**证明黑名单联动功能本身是好的**，包括多条
「列入中」记录的场景。

**另一个坑**：master 上此前显示测试通过，是 `go test` 结果缓存所致。
排查这类问题请用 `go test -count=1`。

---

## 2026-07-26 · 首页饼图修复 + 快捷入口紧凑化

**饼图两个 bug**（此前饼图完全画坏：圆心显示 0、占比显示 48366800%、
6 个扇区退化成互相覆盖的整圆）：

1. **前后端字段命名不匹配**。后端返回 `total_amount`/`total_count`（下划线），
   前端读 `totalAmount`/`totalCount`（驼峰）→ 全是 undefined →
   `total = undefined || 1` → 所有扇区角度除以 1 → 角度爆表。
   **注意**：本项目后端 JSON 统一用下划线命名，前端对接时不要想当然写驼峰。
2. **状态筛选点了没反应**。`watch([groupBy, statusPicked], ...)` 中
   `statusPicked` 是数组 ref，而 `toggleStatus` 用 `push`/`splice` 原地修改，
   引用不变，Vue 默认浅监听不触发。**改法**：加 `{ deep: true }`。

**快捷入口**（`b5fbde1`）：移到整行后仍是 2 列布局，每个卡片撑到半屏宽、
整块占约 350px 高。改为 `repeat(auto-fill, minmax(152px, 1fr))` 自适应铺满 +
横向紧凑排布，8 个模块占 82px。实测扩展到 16 个模块也只占 3 行 127px，
**以后加模块不用改代码**。

---

## 2026-07-25 · 合同额分布改环形饼图 + 地区维度

`b528268` `dd9b6a6`

- 图表由横向条形图改为**环形饼图**，位置与快捷入口对调（饼图移到地图右侧）
- 新增**按地区/按国别**切换（地区口径见 `frontend/src/data/regions.ts`），
  按地区时点击扇区或图例可下钻查看国别明细
- 新增**状态筛选**：全部（单选）+ 在建/未开工/完工（可多选取并集）
- 饼图用**纯 SVG 手绘，不引入任何图表库** —— 离线可用，也利于鸿蒙移植

**⚠️ 配色约束**：分类色取自经校验的色序，固定顺序不循环复用。
分组超过 7 个时尾部合并为「其他 N 项」，**不得生成新色相**（新色相在色盲下
无法与既有色区分）。图例必须显示名称+金额+占比文字作为兜底。

**需求变更**：此项撤销了 requirement.md 中原「不展示国别柱状图」的已确认决策，
变更原因已记录在 requirement.md 的「需求变更记录」表中。

---

## 2026-07-25 · 前端整体重新设计

`d2fbc4c` `f23428a` `399a4c7` `e1bcaef` `fc70139`

Data-Dense Dashboard 风格，深蓝主色 `#1E40AF` + 琥珀强调 `#D97706`，
设计令牌在 `frontend/src/theme.css`（同时覆盖 Element Plus 的 CSS 变量）。
**系统字体栈，无外部字体依赖**（离线/内网可用，也利于鸿蒙移植）。

主要页面：
- **App.vue**：品牌区、分组导航（主业务/扩展模块）、待建模块可点击提示
- **Dashboard.vue**：KPI 卡片（可点击跳转筛选、显示占比、0 值弱化）、
  全球分布地图（国别近似定位 + 空状态）、合同额分布饼图、快捷入口
- **ProjectList.vue**：合同额亿/万自动换算、排序、分页、统计条、筛选标签、
  操作列单行紧凑布局
- **Placeholder.vue**：友好的「模块建设中」空状态

**⚠️ 前端只有一个目录 `frontend/`**。历史上曾短暂并存 `frontend/` 与
`frontend-v2/`，两者 `vite.config.ts` 都输出到 `../backend/frontend-dist`，
谁后构建谁生效且无任何报错 —— 会静默覆盖。已合并删除，**不要再建平行的
前端目录**。旧版 UI 如需查阅：`git show 0b48c56:frontend/src/App.vue`。

#### 状态配色的重要约束

`在建 #2a78d6` / `未开工 #eda100` / `停工 #d03b3b` / `完工 #0ca30c`

- 正常视力下四色两两可辨（最差 ΔE 24.1）
- **但红/绿在红绿色盲下无法靠色相区分（ΔE 仅 4.1，物理限制，换色解决不了）**
- 因此**凡用状态色处必须同时给出文字或形状**：地图标记用四种形状且 tooltip
  带状态文字、表格状态列带形状+文字、KPI 卡带图标+文字、图例带文字
- `未开工 #eda100` 白底对比度仅 2.11（<3:1），**只可用于色块/描边，不可用于文字**

---

## 2026-07-24 · 代码清理与早期 Bug 修复

`7121bf7` 及后续

### 一、代码清理（不改变功能）

| 改动 | 为什么 |
|---|---|
| 合并 `scanProject` / `scanProjectFromRows` 两个近乎相同的 60 行函数为一个 `scanProject(rowScanner)` | 原本两份代码逐字重复，改字段要改两处、极易漏改。`*sql.Row` 和 `*sql.Rows` 都实现了 `Scan(dest ...any) error`，用一个只含 Scan 方法的接口即可统一 —— 这是 Go 的标准做法，**不是"语言限制导致无法合并"** |
| 删除 `services/filewatch.go`，新增 `services/rootdir.go` | 原 FileWatcher 从未接线使用：`onChange` 回调始终为 nil，且 `addRecursive` 名不副实（只监控顶层一层）。它实际承担的唯一职责是「线程安全地保存文件根目录」，故按真实职责改名为 `RootDir`，并从 `go.mod` 移除 `fsnotify` 依赖 |
| 移除误提交的 `opims.exe~` / `stderr.log` / `stdout.log`，`.gitignore` 补 `*.log`、`*~`、`node_modules/` | 加 `.gitignore` 不会移除已跟踪的文件，必须配合 `git rm --cached` |

**注意**：若将来需要「文件变动自动刷新前端」，不要简单恢复 FileWatcher —— 递归监控云盘大目录会在启动时挂载数千个 watch，拖慢启动并可能触及系统上限。应配合 SSE/WebSocket 单独设计，并限制监控深度、去掉逐文件日志。

### 二、Bug 修复

#### 1. 国别判定结果会随机跳变（严重）

- **原因**：`extractCountry` 用 `map[string]string` 存关键词表，而 **Go 的 map 遍历顺序是随机的**。项目名若同时命中多个关键词，返回值会在多次运行间变化。
- **修复**：改为有序切片 `countryRules`（`services/country.go`），按「长国名 → 短国名 → 项目专有缩写」排序；补充越南等 40+ 国别；新增 `country_test.go` 锁定顺序敏感场景。
- **同时移除**：`"LNG" → 阿联酋` 这条规则。LNG 是工艺缩写不是地名，会错误命中任何含 LNG 的项目。
- **注意**：改关键词表后必须跑 `go test ./services/`；且**已入库数据不会自动更新**，需重新导入 Excel。

#### 2. `/api/projects` 无法取全量数据

- **原因**：不带 `status` 参数时后端强制附加 `AND project_status IN ('在建','未开工')`，首页做全量统计时拿不到停工/完工项目。
- **修复**：支持 `status=all` 显式请求全部状态，跳过该默认过滤。原有默认行为保持不变（兼容既有前端调用）。

#### 3. 备份可能丢失最新数据

- **原因**：SQLite 处于 WAL 模式，备份只复制 `.db` 主文件，未落盘的 WAL 数据会丢。
- **修复**：`Backup` 复制前执行 `PRAGMA wal_checkpoint(TRUNCATE)`。

> 前端重新设计与状态配色约束见上方「2026-07-25 · 前端整体重新设计」一节。

改配色时请重新校验，不要凭感觉选色。

---

## v0.1（master 初始版本）

首个可用版本：项目清单、项目文件、分包商黑名单、首页看板四个模块，Excel 导入导出，数据库备份恢复。
