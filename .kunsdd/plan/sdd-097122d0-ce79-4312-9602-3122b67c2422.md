# OPIMS — 遗留功能增量实现计划

## Summary

系统已完成度很高（20条API路由 + 7张DB表 + 10个前端路由 + 6个完整视图）。本计划只覆盖SDD中确认为缺失的增量功能。文件内嵌预览已由用户确认暂不实现。

## 约定：快照机制

我们约定以下组合方案应对上下文压缩：

### 压缩前（你说"要做快照了"）
1. 我在本文件 **Checkpoint** 节写入当前进度快照（已完成/进行中/下一步）
2. 同步更新 todo list，清理到不超过15个活跃项
3. 更新本文件 Steps 节反映最新完成状态

### 压缩后（你说"恢复快照"）
我会依次执行：
1. 读取本文件的 **Checkpoint** 节，了解压缩前的准确进度
2. 读取 **Current State** 表，重建各模块完成度认知
3. 读取整体 **Steps** 清单，确认下一条待办项
4. 读取 **约定** 节本身，恢复压缩机制的上下文
5. 口头确认当前状态给你："当前进展到 X，下一步是 Y，继续？"

## Checkpoint (2026-07-25 18:00)

已完成的项：
- 代码审查完成，确认了所有已有实现
- 增量计划经3轮修订，已定稿
- 文件内嵌预览已排除

下一条待办：
- 实现 `GET /api/dashboard/contract-distribution` 后端接口

## Current State

| 模块 | 状态 | 待完成 |
|------|------|--------|
| 首页看板 | Dashboard.vue 26KB已有SVG饼图+地图+KPI卡片 | 缺专用API：contract-distribution, region-detail |
| 项目清单 | ProjectList.vue 25KB完整CRUD+导入导出+亿/万+GPS | 缺导入冲突保留两者 |
| 项目分包 | subcontract.go 18KB完整+ SubcontractList.vue 6KB | 核验详情6分组+黑名单行标记 |
| 分包商库 | subcontractors.go 6KB列表+详情 | 缺导入模板API+合作历史导出 |
| 分包商黑名单 | blacklist.go 5.8KB完整CRUD+审计日志写入+级联 | 缺前端审计日志UI |
| 项目文件 | 本机打开+搜索+刷新 | 文件预览暂不实现 |
| 其余4模块 | Placeholder | 保持占位 |

## Pre-mortem

| # | 风险 | 等级 | 缓解 |
|---|------|------|------|
| T1 | 饼图>7分类色盲不可区分 | Launch-Blocking | SDD固定7色序，第8起合并其他N项，图例显示文字 |
| T2 | 未开工#eda100对比度2.11 | Launch-Blocking | 仅用于色块/描边 |

## Steps (MoSCoW)

### Must：Dashboard 饼图API
- GET /api/dashboard/contract-distribution?dim=region&status= — 按地区/国别汇总合同额，过滤境外未删除，>7合并其他N项 (covers: 板块要求1.3)
- GET /api/dashboard/region-detail/{region}?status= — 地区下钻国别 (covers: 板块要求1.3)
- backend/data/regions.go 新建 — 地区国别映射 (covers: 新增国别时维护)
- 前端Dashboard.vue对接饼图API + 地图形状颜色双重区分 (covers: 板块要求1.2,1.3)
- 快捷入口底部整行 (covers: 板块要求1.4)

### Must：分包商库导入模板
- POST /api/subcontractors/import — 解析2-sheet模板 (covers: 板块要求5.1)
- GET /api/subcontractors/export/template — 导出空模板 (covers: 板块要求5.1)
- GET /api/subcontractors/:id/export-projects — 导出合作清单 (covers: 板块要求5.3)
- 注册3条路由到main.go
- 核验SubcontractorLib.vue 5分组展示+黑名单标签 (covers: 板块要求5.3,5.4)

### Must：项目分包详情+黑名单标记
- 核验SubcontractByID返回32字段 (covers: 板块要求4.3)
- SubcontractList.vue 详情6分组 (covers: 板块要求4.3)
- 黑名单禁用行背景#f5f5f5+tooltip (covers: 板块要求4.5)
- 核验列表维度切换+统计行 (covers: 板块要求4.2)

### Should：黑名单操作日志UI
- SubBlacklist.vue 操作日志按钮→调用SubBlacklistByID审计 (covers: 板块要求6.3)
- rowClass限制等级颜色 (covers: 板块要求6.5)
- 筛选国别/项目/状态 (covers: 板块要求6.4)
- 拉出后锁定403 (covers: 板块要求6.3)

### Could：导入冲突保留两者
- ImportProjects冲突检测+conflict_mode参数 (covers: 总体要求4)
- ProjectList.vue 冲突对比UI+3选项 (covers: 总体要求4)

### Won't
- 项目进度/质量/人员/人员黑名单 → Placeholder
- 文件内嵌预览 → 用户确认暂不实现

## 文件修改清单

| 文件 | 操作 |
|------|------|
| backend/data/regions.go | 新建 |
| backend/handlers/dashboard.go | 修改 |
| backend/handlers/subcontractors.go | 修改 |
| backend/main.go | 修改 |
| frontend/src/views/Dashboard.vue | 修改 |
| frontend/src/views/SubcontractList.vue | 修改 |
| frontend/src/views/SubcontractorLib.vue | 修改 |
| frontend/src/views/SubBlacklist.vue | 修改 |
| frontend/src/views/ProjectList.vue | 修改 |

## 测试计划

- go test ./services/
- 手动验证：Dashboard饼图、分包商库导入模板、黑名单操作日志、分包详情6分组、导入冲突

## 需求变更校验

| # | 内容 | 状态 |
|---|------|------|
| 合同额饼图 | 地区切换+多选筛选+下钻 | ⏳ |
| 文件监控不做 | 已有手动刷新 | ✅ |
| 国别代表坐标虚线 | 地图形状颜色 | ⏳ |
| 颜色形状双重区分 | 色盲要求 | ⏳ |
| 项目清单显示增强 | 亿/万+排序+分页 | ✅ |
| 分包模块架构 | 缺导入模板API | ⚠️ |
| 文件内嵌预览 | 用户确认暂不实现 | ❌ |
