# OPIMS — requirement.md 状态同步

## 摘要

用户指示 Steps 1-5 全部 Hold。本计划仅执行 requirement.md 的 5 处修改使其与当前代码完成状态一致。

---

## Step 1: 在「需求变更记录」表后、「尚未实现的既有需求」前插入「板块完成状态」节

在 `### 尚未实现的既有需求` 之前插入以下内容：

```markdown
### 板块完成状态（2026-07-26）

| 板块 | 状态 | 说明 |
| ---- | ---- | ---- |
| 首页看板 | ✅ 完成 | 环形饼图、地区/国别切换、状态筛选、下钻 |
| 项目清单 | ✅ 完成 | GPS 解析、亿/万换算、分页、冲突处理（含保留两者） |
| 项目文件 | ✅ 完成 | 根目录 `…\海外运营中心\`、合同识别、文件名搜索 |
| 项目分包 | ✅ 完成 | 账期快照、双维度切换、6 分组详情、黑名单联动 |
| 分包商库 | ✅ 完成（2026-07-26 重构） | 单 Sheet 31 列、编号 upsert、资质文件扫描、关联单位 |
| 分包商黑名单 | ✅ 完成 | 三级限制等级+背景色、拉出锁定、审计日志界面 |
| 项目进度 | ⏸️ Hold | Placeholder，待用户开启 |
| 项目质量 | ⏸️ Hold | Placeholder，待用户开启 |
| 项目人员 | ⏸️ Hold | Placeholder，待用户开启 |
| 人员黑名单 | ⏸️ Hold | Placeholder，待用户开启 |
| 文件内嵌预览 | ⏸️ 暂缓 | ViewFile 仅调用 rundll32 |
```

## Step 2: 修正「分包商库」行

将旧行：
```
| **分包商库**板块 | 板块要求 5 | ✅ 已实现。支持库管理（CRUD）、法代/联系人维护、导入导出模板（2 Sheet）、合作历史聚合… |
```
替换为：
```
| **分包商库**板块 | 板块要求 5 | ✅ 已实现（2026-07-26 重构）。单 Sheet 31 列模板、按编号 upsert、导出/模板下载、合同数与总金额汇总、关联单位标志、资质文件扫描、按关联单位关系级联 |
```

## Step 3: 修正「分包商黑名单与项目分包的自动关联」行

将旧行：
```
| **分包商黑名单与项目分包的自动关联** | P0-13 | ✅ 已实现。`services/blacklist_sync.go` 处理国内母公司-当地公司级联禁用逻辑… |
```
替换为：
```
| **分包商黑名单与项目分包的自动关联** | P0-13, 板块要求 5.4 | ✅ 已实现。`services/blacklist_sync.go` 按「关联单位」关系级联禁用（2026-07-26 变更：原母公司-当地公司关系已废弃） |
```

## Step 4: 表末新增 4 行未修复 Bug

在「分包商黑名单的限制等级」行之后，表结束之前，新增：
```
| B2 世界地图离线底图 | 测试发现 | 🔧 未修复。Leaflet 依赖在线瓦片，断网空白 |
| B3 分包列表无分页 | 测试发现 | 🔧 未修复。`total_count = len(records)`，忽略 page/page_size |
| B5 文件接口路径遍历隐患 | 测试发现 | 🔧 未修复。`ViewFile` 未校验 `..` |
| B7 审计日志 detail 为空 | 测试发现 | 🔧 未修复。仅记录动作类型与时间，未记录变更内容 |
```

## Step 5: 删除遗留 .bak 文件

```bash
rm "D:/OPIMS/.kunsdd/requirements/097122d0-ce79-4312-9602-3122b67c2422/requirement.md.bak"
```

## 验证

- [ ] requirement.md 包含「板块完成状态」节，含 11 行状态表
- [ ] 分包商库行不再提及 "2 Sheet" / "母公司-当地"
- [ ] 关联行改为 "关联单位" 关系
- [ ] B2/B3/B5/B7 行存在于表中
- [ ] `requirement.md.bak` 已删除
